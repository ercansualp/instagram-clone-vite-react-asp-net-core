export type User = {
    id: number,
    username: string,
    avatar: string,
    fullName: string,
    postCount: number,
    followCount: number,
    followersCount: number,
    verified: boolean
}